services:
  afk-bot:
    build: .
    container_name: aternos-afk-bot
    restart: unless-stopped
    environment:
      - MC_HOST=jerome123pogi.aternos.me
      - MC_PORT=60441
      - MC_USERNAME=RedOne
    volumes:
      # Persist Microsoft auth tokens so you don't have to re-login
      - ./auth:/app/auth
    stdin_open: true
    tty: true
